name: CI

on:
  pull_request:
    branches:
      - '**'
    paths-ignore:
      - '.vscode/**'
      - 'docs/**'
      - '.editorconfig'
      - '.env.example'
      - '.gitignore'
      - 'README.md'

jobs:
  run-checks:
    name: Quality
    uses: ./.github/sub-workflows/quality.yml

  run-tests:
    name: Tests
    uses: ./.github/sub-workflows/tests.yml

  run-build:
    name: Build
    uses: ./.github/sub-workflows/build.yml

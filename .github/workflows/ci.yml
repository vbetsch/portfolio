name: CI

on:
  pull_request:
    branches:
      - '**'
    paths-ignore:
      - '.vscode/**'
      - 'docs/**'
      - '.editorconfig'
      - '.env.example'
      - '.gitignore'
      - 'README.md'

jobs:
  run-checks:
    name: Quality
    uses: ./.github/ci/quality.yml

  run-tests:
    name: Tests
    uses: ./.github/ci/tests.yml

  run-build:
    name: Build
    uses: ./.github/ci/build.yml
